#include "get_obj.h"
#include "template.h"

/*
* Создаёт путь до шаблона объекта из пути к объекту
* @param путь до объекта
* return путь до шаблона
*/
static void transform_object_path_to_template_path() {

}

/*
* Находит объект в файле
* @param имя файла
* @param имя шаблона объекта
* @param имя объекта
* return объект в виде свойство-значение
*/
static void find_obj_in_file() {

}

/*
* Печатает объект
* @param объект в виде свойство-значение
*/
static void print_object() {

}

void get_obj(char* path) {
	transform_object_path_to_template_path();
	find_template("lelel");
	find_obj_in_file();
	print_object();
}